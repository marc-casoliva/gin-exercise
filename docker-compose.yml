version: '3.7'

services:
  db:
    image: postgres:13
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: s3cr3t
      POSTGRES_DB: gin-exercise
    ports:
      - "5432:5432"
# Commenting it for dev purposes
#     volumes:
#       - db_data:/var/lib/postgresql/data

# volumes:
#   db_data:
